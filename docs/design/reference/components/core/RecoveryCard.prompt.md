Coral-tinted recovery-guidance card shown under the balance hero when behind; copy is imperative and concrete.

```jsx
<RecoveryCard>
  <span style={{color:'var(--color-negative)',fontWeight:700}}>1,200 behind.</span> Even in <strong style={{color:'var(--text-body)'}}>3 days</strong> spending nothing — or by <strong style={{color:'var(--text-body)'}}>Feb 14</strong> at typical pace. A cheap week beats a perfect day.
</RecoveryCard>
```

Background rgba(240,114,133,.1), border rgba(240,114,133,.35), radius 8px.
