Aqua back link ("‹ Back") that pops a pushed detail pane; sits at the top-left of a detail hero.

```jsx
<BackLink onClick={pop} />
```

Hover brightens aqua #49A5E6 → #5BBEFF. The only navigation affordance besides row taps — the system has no tab bars or menus inside views.
