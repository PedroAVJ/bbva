/** Coral-tinted guidance card for recovery copy ("1,200 behind. Even in 3 days…"). */
export interface RecoveryCardProps {
  /** Recovery copy; bold key figures inline with <strong> */
  children: React.ReactNode;
}
export declare function RecoveryCard(props: RecoveryCardProps): JSX.Element;
