Uppercase section header row ("LAST 14 DAYS · DAILY NET") with an optional faint right annotation; use above every content section.

```jsx
<SectionTitle right="sample outlier: −1,200">Last 14 days · daily net</SectionTitle>
```

Label is auto-uppercased by styling; write it in sentence case. Right slot is 11px faint tabular numerals.
