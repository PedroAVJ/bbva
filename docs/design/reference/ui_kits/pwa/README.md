# Bancomer PWA UI kit

Installable web-app ledger: opaque navy header with the "Bancomer" type wordmark (no logo — see root readme), the same home → category-detail push panes as iOS, an opaque navy "＋ Add transaction" action, and an opaque form sheet — no fake Liquid Glass, no blur anywhere. Manual, offline, on-device model; synthetic sample data from `../shared-data.js`.
